#include "main.h"

/**
 * add - that adds two integers and returns the result.
 * @a: firrst number
 * @b: the sec number
 * Return: the result c
 */

int add(int a, int b)
{
	int c;

	c = a + b;
	return (c);
}
